; Aurora Relay Windows installer customization.
; Electron Builder owns the primary NSIS generation. Keep this file for enterprise
; customization hooks and document any local policy changes here.
!ifndef APP_INSTALLER_CONTEXT
!define APP_INSTALLER_CONTEXT "Aurora Relay"
!endif
