FROM node:20-slim

RUN useradd --create-home --uid 10001 sandbox && mkdir -p /workspace && chown -R sandbox:sandbox /workspace
WORKDIR /workspace
ENV NODE_ENV=production
USER sandbox
CMD ["sleep", "infinity"]
